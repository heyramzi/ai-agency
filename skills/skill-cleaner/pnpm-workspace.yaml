allowBuilds:
  esbuild: true

overrides:
  nanoid@>=3.0.0 <3.3.18: ">=3.3.18 <4"
  # WHY: security version floors (Dependabot). Range-scoped so a bare
  # floor does not over-bump a major line.
  esbuild@<0.25.0: ">=0.25.0 <0.26"
  vite@<6.4.3: ">=6.4.3 <7"
  vitest@<3.2.6: ">=3.2.6 <4"
